import blockContent from './blockContent'
import category from './category'
import post from './post'
import author from './author'
import listing from './listing' // Add this line
import agent from './agent' // Add this line

export const schemaTypes = [post, author, category, blockContent, listing, agent]
